---
services:
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    ports:
      - "80:80"

  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    pid: host